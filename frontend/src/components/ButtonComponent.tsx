import { SaveButton } from "./SaveButton";
import { Button } from "react-bootstrap";
export class ButtonComponent extends SaveButton{
    /*
    render(){
        return(
        <Button variant={this.props.variant||"primary"} type={this.props.type||"submit"}>
        {this.props.caption}
        </Button>
        );
    }
        */
}